@page "/data-status"
@inject NavigationManager Nav

@*
    Tidligere standalone status-side. Konsolidert til /data-import (Status-fanen)
    per brukerinput 2026-05-03. Ruten beholdes for tilbakekompabilitet.
*@

@code {
    protected override void OnInitialized()
    {
        Nav.NavigateTo("data-import?tab=status", replace: true);
    }
}
